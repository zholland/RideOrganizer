<% if @error_msg == nil %>
var travellers = <%= session[:travellers].to_json %>;
var travellerIds = updateTravellers(travellers);
initEditable(travellerIds);
$('#error-box').remove();
<% else %>
$('#error-box').remove();
$('#main-content').prepend('<div id="error-box" class="alert alert-danger-no-margin" role="alert"><%= escape_javascript(@error_msg.to_s)%></div>');
$("html, body").animate({scrollTop: 0}, 'fast');
<% end %>